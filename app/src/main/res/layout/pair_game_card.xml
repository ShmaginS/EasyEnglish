<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <variable
            name="imageState"
            type="com.shmagins.easyenglish.ImageState" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:background="@drawable/pair_game_card_shape"
        android:visibility="@{imageState.isOpened ? View.VISIBLE : View.INVISIBLE}">

        <ImageView
            android:id="@+id/memory_image"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:scaleType="centerInside"
            android:background="@drawable/pair_game_card_shape"
            android:backgroundColor='@{imageState.isSelected ? "#FF00FF" : "#00FFFFFF"}'
            android:src='@{imageState.resource}'
            android:fadeOut="@{imageState.isOpened ? false : true}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintDimensionRatio="1"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>